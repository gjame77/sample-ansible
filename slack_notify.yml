- name: Notify Slack
  hosts: localhost
  gather_facts: false
  vars:
    slack_webhook_url: "https://hooks.slack.com/services/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"
    slack_message: AAP 작업 완료!
  tasks:
    - name: Send Slack message
      uri:
        url: "{{ slack_webhook_url }}"
        method: POST
        headers:
          Content-Type: "application/json"
        body: "hello"
        body_format: json
