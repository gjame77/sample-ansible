- name: Install nginx
  hosts: all
  become: yes
  tasks:
    - name: Install nginx package
      package:
        name: nginx
        state: present

    - name: Verify nginx installed
      command: rpm -q nginx
      register: result

    - debug:
        var: result.stdout
